<!--
  Thanks for contributing! A few things to know first:
  - External PRs are accepted for packages/core and packages/cli (Apache-2.0).
  - The desktop app (src/, src-tauri/) is proprietary; we can't merge outside
    PRs to it. See CONTRIBUTING.md.
-->

## What and why

<!-- What does this change do, and what problem does it solve? Link any issue. -->

Closes #

## Checklist

- [ ] Scope is `packages/core` and/or `packages/cli` (not the desktop UI)
- [ ] Tests added or updated, and `npm run test` passes for the touched area
- [ ] `npm run lint` passes (boundary rules included)
- [ ] `npm run typecheck` passes
- [ ] Commits are signed off (`git commit -s` / DCO) and use Conventional Commits
